import '../styles/PageNotFound.css';
import { Link } from "react-router-dom";

const PageNotFound = ({ id, name, statements }) => {

    return (
        <Link to="/" >Wróć do głownej</ Link >
    );
}

export default PageNotFound;
